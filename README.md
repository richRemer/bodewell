Configuration
=============
Configuration is read from `/etc/bode/config` and from `$PWD/.env`.

```
host=domain.com
port=1234
user=root
group=root
cert=/path/to/cert
key=/path/to/key
cat=/path/to/ca
include=/include/**/pattern.*
```
